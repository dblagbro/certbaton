namespace CertBaton.Domain.Renewals;

public enum RenewalStageOutcome
{
    Succeeded = 0,
    Failed = 1,
    Cancelled = 2,
}

public enum RenewalTerminalOutcome
{
    Succeeded = 0,
    Failed = 1,
    Cancelled = 2,
}
